# 台灣股票分析工具 - 依賴套件
# 註：僅列出程式碼實際 import 的套件。
# tensorflow / torch / ta-lib / bokeh / langchain / openai 已移除（程式碼並未使用，
# 且 ta-lib 在 Windows 極難安裝）。若日後真的導入這些模型再加回。

# 資料處理
pandas>=1.5.0
numpy>=1.21.0
python-dateutil>=2.8.0

# 資料庫（預設使用 SQLite，內建於 Python）
sqlalchemy>=1.4.0
# 若要改用 PostgreSQL，取消下一行註解：
# psycopg2-binary>=2.9.0

# Web框架
fastapi>=0.68.0
uvicorn>=0.15.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 資料取得
yfinance>=0.2.0
requests>=2.25.0
aiohttp>=3.8.0

# 機器學習與統計
scikit-learn>=1.0.0
xgboost>=1.6.0
statsmodels>=0.13.0

# 視覺化 / 前端儀表板
plotly>=5.0.0
streamlit>=1.20.0

# 工具
python-dotenv>=0.19.0
loguru>=0.6.0
schedule>=1.1.0
APScheduler>=3.9.0

# 測試
pytest>=7.0.0
pytest-asyncio>=0.18.0
httpx>=0.23.0

# 開發工具
black>=22.0.0
flake8>=4.0.0
isort>=5.10.0
mypy>=0.900
